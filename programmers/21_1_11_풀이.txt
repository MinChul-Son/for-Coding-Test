21/1/11 프로그래머스 2020 카카오 블라인드 채용 - 문자열압축
------------------------------------------------------------
논리:

슬라이싱하는 값은 최대 절반
for문 돌림 
내부에 for문 한번 더
외부 for문에서는 슬라이싱할 단위인 i 값을 지정하고
:i 를 통하여 기준 문자열을 지정
내부 for문에서 그 기준 문자열을 그 뒷값과 비교한다.
같으면 count +1 
else이고 if count가 1이면 반복되지 않으므로 count를 ""으로 바꿈
result 에 str(count)와 기준문자열을 더함.
기준 문자열을 그 뒷값으로 해주고 count를 1로 해준다.


#lambda식 표현에 익숙해지자.
isdigit() ==> 문자열이 숫자로만 이루어져있는지 판별하는 함수.